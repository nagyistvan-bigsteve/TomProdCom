export enum OrderStatus {
  OFFER = 1,
  CONFIRMED = 2,
}

export enum Unit_id {
  BUC = 1,
  M2 = 2,
  M3 = 3,
  BOUNDLE = 4,
}

export enum Size_id {
  NORMAL = 0,
  EXTRA = 1,
  EXTRA2 = 2,
}

export enum Category {
  A = 1,
  AB = 2,
  B = 3,
  T = 4,
}

export enum ClientType {
  PF = 1,
  PJ = 2,
}
